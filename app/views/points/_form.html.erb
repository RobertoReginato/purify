<%= form_with(model: point, local: true) do |form| %>
  <% if point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(point.errors.count, "error") %> prohibited this point from being saved:</h2>

      <ul>
      <% point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :x_cord %>
    <%= form.text_field :x_cord %>
  </div>

  <div class="field">
    <%= form.label :y_cord %>
    <%= form.text_field :y_cord %>
  </div>

  <div class="field">
    <%= form.label :z_cord %>
    <%= form.text_field :z_cord %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
